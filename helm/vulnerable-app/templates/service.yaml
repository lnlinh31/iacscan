apiVersion: v1
kind: Service
metadata:
  name: {{ include "vulnerable-app.fullname" . }}
  labels:
    {{- include "vulnerable-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vulnerable-app.selectorLabels" . | nindent 4 }}
